import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(255, 0, 88, 1.0);
  static const light = Color.fromRGBO(140, 140, 140, 1.0);
  static const medium = Color.fromRGBO(150, 150, 150, 1.0);
  static const dark = Color.fromRGBO(0, 0, 0, 1.0);
  static const white = Color.fromRGBO(255, 255, 255, 1.0);
}
